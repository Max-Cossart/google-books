import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <div className={styles.footer}>
      <p className={styles.description}>Designed and Built by Max Cossart ©</p>
    </div>
  );
};

export default Footer;
